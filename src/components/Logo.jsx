import logo from "../images/logo.png";

const Logo = () => {
  return (
    <div className="container-logo">
      <img className="logo" src={logo} alt="logo" />
    </div>
  );
};

export default Logo;
